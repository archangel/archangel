<tr>
  <td><%= page.title %></td>
  <td><%= page.slug %></td>
  <td><%= page.published_at %></td>
  <td><%= link_to 'Show', manage_page_path(page) %></td>
  <td><%= link_to 'Edit', edit_manage_page_path(page) %></td>
  <td><%= link_to 'Discard', manage_page_path(page), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
