<% content_for :section do %>
  <h1 class="h2"><%= t('collection_entries.titles.new') %></h1>

  <%= render partial: 'buttons' %>
<% end %>

<%= render 'form', collection: @collection, collection_entry: @collection_entry %>
